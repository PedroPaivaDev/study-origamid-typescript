function pluginSlide(seletor: string) {
  console.log(`Criar slide: ${seletor}`);
}

const Pedro: Usuario = { //está acessando a interface declarada globalmente no arquivo 'script.ts'
  nome: 'Pedro',
  id: 5
}

export default pluginSlide;